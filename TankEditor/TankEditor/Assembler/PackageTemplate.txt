// swift-tools-version: 6.2
import PackageDescription

let package = Package(
    name: "UserCodeDylib",
    platforms: [
        .macOS(.v14)
    ],
    products: [
        .library(
            name: "UserCodeDylib",
            type: .dynamic,
            targets: ["UserCodeDylib"]
        ),
    ],
    dependencies: [
        // если не нужен TankEngine2D как spm-пакет — убираем
        // .package(path: "Frameworks/TankEngine2D")
    ],
    targets: [
        .target(
            name: "UserCodeDylib",
            dependencies: [
                // если TankEngine2D как spm-пакет не нужен — остаётся пустым списком
            ],
            swiftSettings: [
                // путь к каталогу с твоим фреймворком
                .unsafeFlags(["-F", "Frameworks"]),

                // линковка фреймворка
                .unsafeFlags(["-framework", "TankEngine2D"]),

                // rpath, чтобы dylib находила фреймворк при запуске
                .unsafeFlags(["-Xlinker", "-rpath", "-Xlinker", "@loader_path/../Frameworks"]),
                .unsafeFlags(["-Xlinker", "-rpath", "-Xlinker", "@executable_path/../Frameworks"])
            ]
        ),
    ]
)
