// Package.swift в /Users/sergeykozlov/Library/Application Support/TankEditor/Sandbox/UserBuild

// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "userbuild",
    platforms: [
        .macOS(.v14)
    ],
    products: [
        .library(
            name: "UserCodeDylib",
            type: .dynamic,
            targets: ["UserCodeDylib"]
        ),
    ],
    dependencies: [
        // Путь к пакету с макросами
        .package(
            name: "tankengine2dmacrosonly",
            path: "../TankEngine2DMacrosOnly"
        )
    ],
    targets: [
        .target(
            name: "UserCodeDylib",
            dependencies: [
                // продукт из пакета с макросами
                .product(
                    name: "TankEngine2DMacroInterfaces",
                    package: "tankengine2dmacrosonly"
                ),
            ],
            path: "Sources/UserCodeDylib",
            swiftSettings: [
                // Компилятор должен знать где искать модуль TankEngine2D
                .unsafeFlags(["-F", "Frameworks"])
            ],

            linkerSettings: [
                // Линкер должен знать где лежит сам фреймворк
                .unsafeFlags(["-F", "Frameworks"]),
                .linkedFramework("TankEngine2D")
            ]
        ),
    ]
)
